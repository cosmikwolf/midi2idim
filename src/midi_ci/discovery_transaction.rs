
// use midi_ci::midi_ci_standard_format_messages;
// use crate midi_ci;
// mod crate::midi_ci::midi_definitions;
// use crate::midi_ci::midi_definitions;

use super::constants_definitions::*;
// mod midi;
// use serde::{Deserialize, Serialize};
// mod midi_definitions;
// #[derive(Serialize, Deserialize)]
// pub struct midi_ci_struct {
// UNIVERSAL_SYSTEM_EXCLUSIVE
// END_UNIVERSAL_SYSTEM_EXCLUSIVE
// MIDI_CI_MSG_SYSTEM_EXCLUSIVE_START,

trait MessageConstructors {
    fn construct_message(
    ) {
        // let message = midi_ci;
        // let bytes = bincode::serialize(&a).unwrap();
        // println!("{:?}", bytes);
        
    }
}

trait MidiCapabilityInquiryMessages {
    
}